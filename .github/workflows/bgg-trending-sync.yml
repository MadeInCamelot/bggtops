name: BGG Trending Sync

on:
  schedule:
    # Every 6 hours. BGG's hot list doesn't move fast enough to need more.
    - cron: '0 */6 * * *'
  workflow_dispatch: {} # lets you trigger a manual run from the Actions tab

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: '24'

      - name: Install dependencies
        run: npm install fast-xml-parser

      - name: Run BGG trending sync
        env:
          BGG_API_TOKEN: ${{ secrets.BGG_API_TOKEN }}
          SHOPIFY_STORE_DOMAIN: ${{ secrets.SHOPIFY_STORE_DOMAIN }}
          SHOPIFY_CLIENT_ID: ${{ secrets.SHOPIFY_TRENDING_CLIENT_ID }}
          SHOPIFY_CLIENT_SECRET: ${{ secrets.SHOPIFY_TRENDING_CLIENT_SECRET }}
        run: node bgg-trending-sync.js
